git init

git add tutorial.txt

man kan også bruke git add.

git add

git commit -message "beskjed"

git commit -m "første commit"

git status

git remote add origin "link til github repo"

git push...